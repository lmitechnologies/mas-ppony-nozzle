version: "3.9"
services:
  yolov8_train:
    container_name: yolov8_train
    build:
      context: .
      dockerfile: dockerfile
    ipc: host
    deploy:
        resources:
          reservations:
            devices:
              - driver: nvidia
                count: 1
                capabilities: [gpu]
    volumes:
      - ../training:/app/training   # training output
      - ../data/resized_yolo:/app/data  # training data
      - ./2023-09-18_dataset.yaml:/app/config/dataset.yaml  # dataset settings
      - ./2023-09-18_train.yaml:/app/config/hyp.yaml  # customized hyperparameters
    command: >
      python3 /repos/LMI_AI_Solutions/object_detectors/yolov8_lmi/cmd.py
